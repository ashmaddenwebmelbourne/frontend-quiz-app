Make sure all of the elements are using correct, semantic HTML
No typescript errors 
 
CSS stuff
light mode dark mode toggle that persist on browser refresh localstorage 
build 
deploy to github pages  
