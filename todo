CSS stuff (BEM, CSS variables) spacing, sizing, everything but colors:
Give all CSS class names first, then do the stlying for mobile and expand up 


  
light mode dark mode toggle that persists on browser refresh localstorage
deploy to github pages  
